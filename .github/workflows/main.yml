name: app

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: npm install

      - run: npm test

      - run: echo "tests passed"
        if: success()

      - run: echo "tests failed"
        if: failure()
